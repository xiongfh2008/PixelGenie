# 🔄 智能故障转移系统

## 📋 概述

实现了一个对用户完全透明的智能模型切换机制。当某个 API 提供商出现异常时，系统会自动按优先级顺序切换到其他可用模型，整个过程对用户无感知。

---

## ✨ 核心特性

### 1. 自动故障转移
- ✅ 当前 API 失败时自动切换到下一个
- ✅ 按优先级顺序尝试（主用 → 备用 → 后备）
- ✅ 最多尝试 3 次不同的提供商
- ✅ 智能跳过已失败的提供商

### 2. 对用户透明
- ✅ 用户无需知道使用了哪个 API
- ✅ 切换过程完全自动化
- ✅ 响应格式统一，不暴露内部细节
- ✅ 只返回最终结果

### 3. 智能健康管理
- ✅ 实时更新 API 健康状态
- ✅ 自动排除不健康的提供商
- ✅ 错误计数和泄露检测
- ✅ 定期健康检查

### 4. 错误处理优化
- ✅ 区分可重试和不可重试的错误
- ✅ 智能延迟策略（避免过快重试）
- ✅ 详细的日志记录（仅服务器端）
- ✅ 友好的错误消息（用户端）

---

## 🏗️ 系统架构

### 组件结构

```
┌─────────────────────────────────────────────────────────┐
│                    用户请求                              │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              智能故障转移系统                            │
│  ┌──────────────────────────────────────────────────┐  │
│  │  1. 选择 API 提供商（按优先级）                  │  │
│  │  2. 构建请求                                      │  │
│  │  3. 发送请求                                      │  │
│  │  4. 解析响应                                      │  │
│  │  5. 如果失败 → 切换到下一个提供商                │  │
│  │  6. 返回成功结果                                  │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│              API 提供商（按优先级）                      │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐             │
│  │  Google  │→ │Cloudflare│→ │HuggingFace│             │
│  │  (主用)  │  │  (备用)  │  │  (后备)  │             │
│  └──────────┘  └──────────┘  └──────────┘             │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│                统一的响应格式                            │
│              （用户无感知切换）                          │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 实现细节

### 1. 核心函数：executeWithFailover

```javascript
async function executeWithFailover(
  apiCallFunction,      // API 调用函数
  params,               // 请求参数
  requiredCapability,   // 所需能力
  selectApiProvider,    // 提供商选择函数
  updateApiHealth,      // 健康状态更新函数
  maxRetries = 3        // 最大重试次数
)
```

**工作流程**:
1. 选择第一个可用的 API 提供商
2. 尝试调用 API
3. 如果成功：更新健康状态，返回结果
4. 如果失败：
   - 标记当前提供商为不健康
   - 选择下一个提供商
   - 重试（最多 3 次）
5. 如果所有提供商都失败：返回友好的错误消息

### 2. API 优先级

```javascript
// 主用提供商（优先级最高）
primaryProviders = ['google', 'xunfei']

// 备用提供商（免费或成本较低）
backupProviders = ['cloudflare', 'huggingface', 'deepseek']

// 后备提供商（最后选择）
fallbackProviders = ['baidu', 'tencent', 'alibaba']
```

### 3. 能力映射

```javascript
capabilitySupport = {
  imageModification: ['google'],  // 只有 Google 支持图像生成
  imageAnalysis: ['google', 'xunfei', 'cloudflare', 'huggingface', 'deepseek'],
  textTranslation: ['google', 'cloudflare', 'huggingface']
}
```

---

## 📊 切换场景示例

### 场景 1: 正常情况

```
用户请求 → Google API → 成功 ✅
响应时间: 2秒
用户体验: 完美
```

### 场景 2: 主用 API 失败

```
用户请求 → Google API → 失败 ❌ (密钥泄露)
         ↓
         Cloudflare API → 成功 ✅
响应时间: 3秒
用户体验: 无感知（稍慢但成功）
```

### 场景 3: 多次切换

```
用户请求 → Google API → 失败 ❌ (密钥泄露)
         ↓
         Cloudflare API → 失败 ❌ (网络超时)
         ↓
         HuggingFace API → 成功 ✅
响应时间: 5秒
用户体验: 无感知（较慢但成功）
```

### 场景 4: 所有 API 失败

```
用户请求 → Google API → 失败 ❌
         ↓
         Cloudflare API → 失败 ❌
         ↓
         HuggingFace API → 失败 ❌
         ↓
         返回错误消息 ❌
响应时间: 8秒
用户体验: 看到友好的错误提示
```

---

## 🎯 使用示例

### 图像分析端点（已集成）

```javascript
app.post('/api/analyze-image', async (req, res) => {
  try {
    // 准备请求参数
    const parts = [...]; // 图像和提示

    // 定义 API 调用函数
    const apiCallFunction = async (provider, params) => {
      const { url, requestBody, headers } = buildAnalyzeImageRequest(
        provider, 
        params, 
        getApiKeys()
      );

      const response = await fetch(url, {
        method: 'POST',
        headers,
        body: JSON.stringify(requestBody),
        signal: AbortSignal.timeout(30000)
      });

      if (!response.ok) {
        throw new Error(`HTTP ${response.status}`);
      }

      const data = await response.json();
      return parseAnalyzeImageResponse(data, provider);
    };

    // 执行带故障转移的调用
    const result = await executeWithFailover(
      apiCallFunction,
      { parts },
      'imageAnalysis',  // 所需能力
      selectApiProvider,
      updateApiHealth,
      3  // 最多尝试 3 次
    );

    // 返回结果（不暴露使用了哪个 API）
    res.json(result.data);

  } catch (error) {
    res.status(500).json({ error: error.message });
  }
});
```

---

## 📈 性能优化

### 1. 智能延迟策略

```javascript
function getRetryStrategy(error) {
  if (error.includes('rate limit')) {
    return { shouldRetry: true, delay: 3000 };  // 3秒
  }
  if (error.includes('timeout')) {
    return { shouldRetry: true, delay: 1000 };  // 1秒
  }
  if (error.includes('invalid input')) {
    return { shouldRetry: false, delay: 0 };    // 不重试
  }
  return { shouldRetry: true, delay: 500 };     // 默认 500ms
}
```

### 2. 并发控制

- 同一请求内的重试是串行的（避免浪费资源）
- 不同用户的请求是并发的（充分利用资源）
- 超时设置：30秒（防止长时间等待）

### 3. 健康状态缓存

- 健康状态在内存中缓存
- 每 5 分钟自动检查一次
- 失败后立即更新状态

---

## 🔍 日志示例

### 成功切换的日志

```
🔄 Attempt 1/3 using provider: google
❌ Provider google failed: Your API key was reported as leaked
🔄 Switching to next available provider...
🔄 Attempt 2/3 using provider: cloudflare
✅ Request succeeded with provider: cloudflare
```

### 用户看到的

```
✅ 成功
（完全不知道发生了切换）
```

---

## 🛡️ 错误处理

### 可重试的错误

- 网络超时
- 速率限制
- 服务器临时错误 (503)
- 连接重置

### 不可重试的错误

- 无效输入 (400)
- 认证失败 (401)
- 权限不足 (403)
- 资源不存在 (404)
- 格式错误

### 特殊处理

- **密钥泄露**: 立即标记为不健康，永久跳过
- **配额超限**: 延迟 3 秒后重试下一个
- **模型不支持**: 直接切换到支持的提供商

---

## 📊 监控和统计

### 服务器端日志

```javascript
{
  timestamp: '2025-11-26T12:00:00Z',
  request_id: 'req_123',
  endpoint: '/api/analyze-image',
  attempts: [
    { provider: 'google', status: 'failed', error: 'API key leaked', duration: 1200 },
    { provider: 'cloudflare', status: 'success', duration: 2100 }
  ],
  total_duration: 3300,
  final_provider: 'cloudflare'
}
```

### 健康状态

```javascript
apiHealthStatus = {
  google: { 
    healthy: false, 
    lastCheck: 1700000000000, 
    errorCount: 5,
    leaked: true 
  },
  cloudflare: { 
    healthy: true, 
    lastCheck: 1700000000000, 
    errorCount: 0 
  }
}
```

---

## 🎯 最佳实践

### 1. 配置多个 API

至少配置 2-3 个 API 提供商以确保高可用性：

```env
GOOGLE_API_KEY=your_key
CLOUDFLARE_API_TOKEN=your_token
CLOUDFLARE_ACCOUNT_ID=your_id
HUGGINGFACE_API_KEY=your_key
```

### 2. 监控健康状态

定期检查 API 健康状态：

```bash
curl http://localhost:3001/api/health
```

### 3. 设置告警

当所有 API 都失败时发送告警通知。

### 4. 定期轮换密钥

避免密钥泄露导致服务中断。

---

## 🚀 集成步骤

### 步骤 1: 导入模块

```javascript
import { executeWithFailover } from './smart-failover.js';
import { 
  buildAnalyzeImageRequest, 
  parseAnalyzeImageResponse 
} from './api-caller.js';
```

### 步骤 2: 定义 API 调用函数

```javascript
const apiCallFunction = async (provider, params) => {
  const { url, requestBody, headers } = buildAnalyzeImageRequest(
    provider, params, getApiKeys()
  );
  
  const response = await fetch(url, {
    method: 'POST',
    headers,
    body: JSON.stringify(requestBody)
  });
  
  if (!response.ok) throw new Error(`HTTP ${response.status}`);
  
  const data = await response.json();
  return parseAnalyzeImageResponse(data, provider);
};
```

### 步骤 3: 使用故障转移

```javascript
const result = await executeWithFailover(
  apiCallFunction,
  params,
  'imageAnalysis',
  selectApiProvider,
  updateApiHealth
);
```

### 步骤 4: 返回结果

```javascript
res.json(result.data);
```

---

## 📚 相关文档

- `server/smart-failover.js` - 故障转移核心逻辑
- `server/api-caller.js` - API 调用封装
- `server/index.js` - 主服务器文件
- `SAFE_GITHUB_PUBLISH.md` - 安全发布指南

---

## ✅ 优势总结

| 特性 | 传统方式 | 智能故障转移 |
|------|---------|-------------|
| API 失败处理 | 直接返回错误 | 自动切换到备用 API |
| 用户体验 | 看到错误消息 | 无感知，只看到结果 |
| 可用性 | 单点故障 | 高可用性 |
| 维护成本 | 需要手动切换 | 自动化 |
| 响应时间 | 快速失败 | 稍慢但成功率高 |

---

## 🎊 总结

智能故障转移系统提供了：

1. ✅ **对用户透明** - 用户无需知道内部切换
2. ✅ **自动化** - 无需人工干预
3. ✅ **高可用性** - 多个 API 互为备份
4. ✅ **智能化** - 根据错误类型智能决策
5. ✅ **可监控** - 详细的日志和健康状态

**现在您的系统具备了企业级的容错能力！** 🚀

